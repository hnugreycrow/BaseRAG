<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnu.backend.observability.mapper.RagStageRunMapper">
    <insert id="insertBatch">
        INSERT INTO rag_stage_runs (
            id, rag_run_id, stage_name, sub_question_id, sequence_no, status,
            input_count, output_count, model_id, provider, model, reason_code,
            error_code, started_at, first_token_at, completed_at, elapsed_ms, ttft_ms
        ) VALUES
        <foreach collection="stages" item="stage" separator=",">
            (
                #{stage.id}, #{stage.ragRunId}, #{stage.stageName}, #{stage.subQuestionId},
                #{stage.sequenceNo}, #{stage.status}, #{stage.inputCount}, #{stage.outputCount},
                #{stage.modelId}, #{stage.provider}, #{stage.model}, #{stage.reasonCode},
                #{stage.errorCode}, #{stage.startedAt}, #{stage.firstTokenAt},
                #{stage.completedAt}, #{stage.elapsedMs}, #{stage.ttftMs}
            )
        </foreach>
    </insert>
</mapper>
