# BaseRAG 产品需求

> 范围基线：Local Multi-user RAG（2026-09-15）

## 1. 文档定位

本文是 BaseRAG **当前产品范围的唯一事实来源**，定义当前版本支持什么、不支持什么以及如何验收。`README.md` 只负责使用入口，`docs/architecture.md` 只描述当前技术实现；两者与本文冲突时，应先确认产品决策，再同步修改本文和相关文档。

## 2. 产品定位与目标

BaseRAG 是一个本地运行、多用户隔离的 RAG 知识问答系统。当前目标是让每个用户在私有数据空间中管理 Markdown 知识资料，通过可审计的模块化 RAG 流水线完成多轮问答。

当前版本面向本地开发与演示，不作为公网生产系统。

## 3. 当前支持范围

### 3.1 知识库管理

- 创建、查看、重命名和删除知识库。
- 创建知识库时，从后端配置的候选中选择并绑定 Embedding 模型及维度。
- 删除知识库时，同时清理所属文档数据，并尽力清理对象存储中的原文件。

### 3.2 文档管理

- 上传不超过 5 MiB 的 UTF-8 `.md` 或 `.markdown` 文件，或不超过 20 MiB 的文本型 PDF、DOCX 文件。
- 上传只保存原文件并创建 `UPLOADED` 版本，不自动分块。
- 管理员手动触发相应格式的解析、统一语义分块、Embedding 和向量入库。
- 查看文档状态、分块列表、分块正文和来源行、页或段落位置。
- 重命名、删除或重新分块文档；重建失败时保留旧的 READY 索引。
- 单份文档最多生成 1000 个分块，同步处理任务在单实例内限制并发。

### 3.3 RAG 问答

- 只检索管理员创建的知识库中当前生效且状态为 `READY` 的文档版本。
- 多轮追问时，结合持久化摘要、未摘要历史和最近对话生成独立问题，并在确有多个独立目标时拆分为数量受限的子问题；规划失败时回退为原问题和单个子问题。
- 对每个子问题执行知识检索、系统闲聊或内部 MCP 工具意图路由；路由失败或低置信度时安全回退到知识检索。MCP 总开关默认关闭且允许列表为空，当前产品没有可供用户调用的工具。
- 每个知识型子问题按知识库绑定的 Embedding 模型独立生成查询向量，使用 pgvector 精确余弦检索，并在冻结预算内保留候选和子问题归因。
- 使用 RRF 按检索名次融合候选，保护各知识型子问题的最低证据覆盖；原始相似度只用于同模型内部排序和审计。
- 在模型重排前执行分块 ID、规范化正文和相邻重叠去重；Reranker 开启时从去重候选中选择最终证据，超时、失败或 noop 时按确定性融合分降级。
- 提示词将会话记忆、问题计划、工具观察和原始问题分为结构化数据区；Top K 证据先按文档版本聚合，以 `<content ref="S*">` 纯文本区提供，不传送文档名、内部 ID 或分数。历史内容和文档正文均不能覆盖系统规则。
- 新回答每个文档版本只分配一个 `S*`，正文引用集中于段落或列表项末尾；分别校验知识来源 `S*` 与工具标记 `T*`，引用非法时只用相同输入修复一次。规范化正文通过 SSE reset 替换流式草稿。
- 返回并持久化回答、实际来源、有效引用、检索问题和模型信息。
- 来源快照包含实际进入提示词的全部文档和原始位置；来源面板只展示回答实际引用的文档。用户可通过本人会话中的已完成回答读取实际引用版本的原文件；旧回答保持原有分块编号与快照，不批量改写。
- 资料不足时明确拒答，不使用会话历史作为事实证据。
- 用户问答默认检索全部管理员创建的知识库；仅在 local profile 开放的单轮兼容接口允许传入 `knowledgeBaseIds`，用于隔离自动评测，不构成面向用户的知识库选择功能。

### 3.4 会话管理

- 创建、搜索、查看、重命名和删除会话。
- 持久化用户消息、回答版本、来源快照和生成尝试，刷新页面后可恢复。
- 通过 SSE 流式返回回答，支持停止、失败重试和最后一轮重新生成。
- 支持切换回答版本；同一会话同时只运行一个生成任务。

### 3.5 API 基础约定

- API 使用 RESTful 资源路径。
- 除 SSE 和 `204 No Content` 外，JSON 响应统一包含 `code`、`message`、`data` 和 `requestId`。
- HTTP 状态码表达请求结果；稳定的业务错误码用于客户端识别错误。
- 参数校验、业务异常、常见 Web 异常和未知异常由全局异常处理器转换。

### 3.6 认证、账号与隔离

- 使用 Sa-Token 1.46.0 和 Redis 保存会话；Redis 不可用时认证失败关闭，不降级到内存。
- 登录使用 8 小时有效、30 分钟无操作超时的 HttpOnly 会话 Cookie；写请求使用 Token Session 中的随机 CSRF nonce。
- 登录失败按规范化用户名和直接客户端地址的哈希组合键执行 Redis 固定窗口限流。
- 首次启动从环境变量创建 ADMIN 并接管历史数据；管理员 API 可创建 ADMIN/USER、禁用账号和重置密码。
- 只有管理员可以创建和管理知识库与文档，所有管理员可共同维护管理员创建的库。知识库 `owner_id` 保留创建者归属，文档处理沿用该值；会话、消息和生成尝试继续按提问者隔离。
- 禁用和重置密码会撤销该用户全部会话并停止活动 SSE；自行改密仅保留一个新当前会话。

### 3.7 单次问答可观测性

- 每个会话回答版本建立唯一运行记录；幂等重放不重复创建，重试和重新生成分别建立新记录。
- 记录记忆与摘要、规划、路由、子问题执行、Embedding、数据库检索、合并、去重、重排、Prompt、回答模型、引用校验和结果持久化阶段。
- 同时计算请求进入后端到首个成功发送 Delta 的端到端 TTFT，以及最终有效回答模型尝试的模型 TTFT；无有效内容时保持为空。
- 链路观测接口仅管理员可访问，并可按用户筛选。Trace 不保存问题、回答、证据、提示词、认证信息或模型密钥，默认保留 30 天。
- Trace 的 `evidence_count` 始终统计进入提示词的证据分块数，不是聚合后的文档数。
- 观测只覆盖会话 SSE 问答、重试和重新生成；`/api/questions` 继续作为不产生 Trace 的评测兼容入口。

## 4. 当前明确不支持

- 扫描 PDF 的 OCR、旧 `.doc`、TXT、图片、音视频等格式。
- 上传后自动处理、持久化异步任务、任务进度、任务取消和重启恢复。
- 注册、邮箱找回、知识库共享范围配置、组织、细粒度 RBAC 和公网部署。
- 面向用户的知识库选择界面；当前问答检索管理员创建的全部公共知识库。
- 关键词或混合检索、近似向量索引和知识图谱。
- 面向用户开放的 MCP、Tool Calling、Agent、工作流和外部联网搜索；现有 MCP 注册、校验和执行代码仅是默认关闭的内部安全框架。

## 5. 核心业务流程

### 文档处理

创建知识库 → 选择 Embedding 模型 → 上传 Markdown → 保存原文件和 UPLOADED 版本 → 用户手动开始分块 → 解析与切片 → 生成 Embedding → 事务写入分块并激活 READY 版本。

### 多轮问答

创建或打开会话 → 输入问题 → 加载会话记忆 → 改写并按需拆分子问题 → 意图路由 → 分预算检索 → RRF 合并与去重 → 模型重排或确定性降级 → 组装结构化提示词 → 流式生成 → 校验并按需修复引用 → 保存回答及审计快照 → 页面展示回答和来源。

## 6. 验收标准

当前版本在满足以下条件时通过验收：

1. 能在本地启动 PostgreSQL/pgvector、RustFS、后端和前端。
2. 能创建知识库并绑定可用的 Embedding 模型。
3. 能上传示例 Markdown，手动分块后状态变为 `READY`。
4. 能在聊天页面完成至少两轮问答，并收到 SSE 增量内容。
5. 新回答按文档版本引用，来源位置能对应实际进入最终提示词的检索分块；旧回答保持旧编号，非法引用最多修复一次。
6. 停止、失败重试、重新生成、版本切换和刷新恢复行为可用。
7. 普通 JSON 接口和错误响应符合统一协议，且带有请求追踪 ID。
8. 后端日常测试、前端测试和前端生产构建通过；真实基础设施和模型验收按 `README.md` 单独执行。

## 7. 后续规划

下一阶段优先考虑持久化异步索引、身份与检索范围隔离、可重复质量评测、文本型 PDF、混合检索和内部部署能力。现有 Reranker 与问题拆分必须先通过固定评测集证明收益；Agent、对用户开放的 MCP 与 Tool Calling 不在近期 RAG 稳定性工作的前置范围内。

`docs/phase-2-plan.md` 是历史路线图，其中部分会话能力已经实现；它不能覆盖本文定义的当前范围，实施前需要重新整理剩余事项和优先级。
