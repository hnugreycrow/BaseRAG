# BaseRAG 产品需求

> 范围基线：Local RAG Pipeline（2026-09-14）

## 1. 文档定位

本文是 BaseRAG **当前产品范围的唯一事实来源**，定义当前版本支持什么、不支持什么以及如何验收。`README.md` 只负责使用入口，`docs/architecture.md` 只描述当前技术实现；两者与本文冲突时，应先确认产品决策，再同步修改本文和相关文档。

## 2. 产品定位与目标

BaseRAG 是一个本地运行、单用户使用的 RAG 知识问答系统。当前目标是让用户能够管理 Markdown 知识资料，通过可审计的模块化 RAG 流水线和大语言模型完成多轮问答，并追踪每次回答使用的来源、引用和模型。

当前版本面向本地开发与演示，不作为公网生产系统。

## 3. 当前支持范围

### 3.1 知识库管理

- 创建、查看、重命名和删除知识库。
- 创建知识库时，从后端配置的候选中选择并绑定 Embedding 模型及维度。
- 删除知识库时，同时清理所属文档数据，并尽力清理对象存储中的原文件。

### 3.2 文档管理

- 上传不超过 5 MiB 的 UTF-8 `.md` 或 `.markdown` 文件。
- 上传只保存原文件并创建 `UPLOADED` 版本，不自动分块。
- 用户手动触发 Markdown 解析、语义分块、Embedding 和向量入库。
- 查看文档状态、分块列表、分块正文和来源行号。
- 重命名、删除或重新分块文档；重建失败时保留旧的 READY 索引。
- 单份文档最多生成 1000 个分块，同步处理任务在单实例内限制并发。

### 3.3 RAG 问答

- 只检索所有知识库中当前生效且状态为 `READY` 的文档版本。
- 多轮追问时，结合持久化摘要、未摘要历史和最近对话生成独立问题，并在确有多个独立目标时拆分为数量受限的子问题；规划失败时回退为原问题和单个子问题。
- 对每个子问题执行知识检索、系统闲聊或内部 MCP 工具意图路由；路由失败或低置信度时安全回退到知识检索。MCP 总开关默认关闭且允许列表为空，当前产品没有可供用户调用的工具。
- 每个知识型子问题按知识库绑定的 Embedding 模型独立生成查询向量，使用 pgvector 精确余弦检索，并在冻结预算内保留候选和子问题归因。
- 使用 RRF 按检索名次融合候选，保护各知识型子问题的最低证据覆盖；原始相似度只用于同模型内部排序和审计。
- 在模型重排前执行分块 ID、规范化正文和相邻重叠去重；Reranker 开启时从去重候选中选择最终证据，超时、失败或 noop 时按确定性融合分降级。
- 提示词将会话记忆、问题计划、知识证据、工具观察和原始问题分为结构化数据区；历史内容和文档正文均不能覆盖系统规则。
- 最终回答复用模型候选回退和 SSE 流式输出，分别校验知识来源 `S*` 与工具标记 `T*`；引用非法时只使用同一结构化输入修复一次。
- 返回并持久化回答、实际来源、有效引用、检索问题和模型信息。
- 资料不足时明确拒答，不使用会话历史作为事实证据。
- 用户问答仍默认检索全部知识库；仅在 local profile 开放的单轮兼容接口允许传入 `knowledgeBaseIds`，用于隔离自动评测，不构成面向用户的知识库选择功能。

### 3.4 会话管理

- 创建、搜索、查看、重命名和删除会话。
- 持久化用户消息、回答版本、来源快照和生成尝试，刷新页面后可恢复。
- 通过 SSE 流式返回回答，支持停止、失败重试和最后一轮重新生成。
- 支持切换回答版本；同一会话同时只运行一个生成任务。

### 3.5 API 基础约定

- API 使用 RESTful 资源路径。
- 除 SSE 和 `204 No Content` 外，JSON 响应统一包含 `code`、`message`、`data` 和 `requestId`。
- HTTP 状态码表达请求结果；稳定的业务错误码用于客户端识别错误。
- 参数校验、业务异常、常见 Web 异常和未知异常由全局异常处理器转换。

## 4. 当前明确不支持

- PDF、TXT、DOCX、OCR、图片、音视频等非 Markdown 文档。
- 上传后自动处理、持久化异步任务、任务进度、任务取消和重启恢复。
- 登录、用户权限、多用户数据隔离和公网部署。
- 用户选择一个或多个知识库作为检索范围；当前为本地单用户全库检索。
- 关键词或混合检索、近似向量索引和知识图谱。
- 面向用户开放的 MCP、Tool Calling、Agent、工作流和外部联网搜索；现有 MCP 注册、校验和执行代码仅是默认关闭的内部安全框架。

## 5. 核心业务流程

### 文档处理

创建知识库 → 选择 Embedding 模型 → 上传 Markdown → 保存原文件和 UPLOADED 版本 → 用户手动开始分块 → 解析与切片 → 生成 Embedding → 事务写入分块并激活 READY 版本。

### 多轮问答

创建或打开会话 → 输入问题 → 加载会话记忆 → 改写并按需拆分子问题 → 意图路由 → 分预算检索 → RRF 合并与去重 → 模型重排或确定性降级 → 组装结构化提示词 → 流式生成 → 校验并按需修复引用 → 保存回答及审计快照 → 页面展示回答和来源。

## 6. 验收标准

当前版本在满足以下条件时通过验收：

1. 能在本地启动 PostgreSQL/pgvector、RustFS、后端和前端。
2. 能创建知识库并绑定可用的 Embedding 模型。
3. 能上传示例 Markdown，手动分块后状态变为 `READY`。
4. 能在聊天页面完成至少两轮问答，并收到 SSE 增量内容。
5. 回答展示的来源、行号和引用能对应实际进入最终提示词的检索片段；非法引用最多修复一次。
6. 停止、失败重试、重新生成、版本切换和刷新恢复行为可用。
7. 普通 JSON 接口和错误响应符合统一协议，且带有请求追踪 ID。
8. 后端日常测试、前端测试和前端生产构建通过；真实基础设施和模型验收按 `README.md` 单独执行。

## 7. 后续规划

下一阶段优先考虑持久化异步索引、身份与检索范围隔离、可重复质量评测、文本型 PDF、混合检索和内部部署能力。现有 Reranker 与问题拆分必须先通过固定评测集证明收益；Agent、对用户开放的 MCP 与 Tool Calling 不在近期 RAG 稳定性工作的前置范围内。

`docs/phase-2-plan.md` 是历史路线图，其中部分会话能力已经实现；它不能覆盖本文定义的当前范围，实施前需要重新整理剩余事项和优先级。
